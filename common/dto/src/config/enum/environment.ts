export enum Environment {
    //Production environment
    Production = "production",
    //Staging is used on pre-production environments (resembled prod environment)
    // Stage = "stage",
    //Test environment (mostly on same environment as develop)
    Test = "test",
    Development = "development",
    Local = "local"
}
